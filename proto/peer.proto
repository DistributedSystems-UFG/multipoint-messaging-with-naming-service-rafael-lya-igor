syntax = "proto3";
package peer;

service PeerService {
  rpc SendMessage (MessageRequest) returns (MessageResponse);
}

message MessageRequest  { string sender = 1; string content = 2; }
message MessageResponse { bool ok = 1; }
